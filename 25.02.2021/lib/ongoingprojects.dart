import 'package:flutter/material.dart';

class onGoingProject extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Ongoing Project'),
      ),
      body: Center(
        child: Container(
          child: Text('Ongoing Project Page'),
        ),
      ),
    );
  }
}