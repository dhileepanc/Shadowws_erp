
import 'package:flutter/material.dart';

class completedproject extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Completed Project'),
      ),
      body: Container(
        child: Text('Completed Project Page'),
      ),
    );
  }
}